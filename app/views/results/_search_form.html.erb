<%= form_with url: results_path do |f| %>
<% end %>

<div class="me-3">
  <%= form_with url: results_path, method: :get do |form| %>
    <%= form.label :query, "Search for:" %>
    <%= form.text_field :query %>
    <%= form.submit "Search", data: { turbo: false } %>
  <% end %>
</div>
